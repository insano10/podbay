- make adding connection work in feed
 - have followed ESS from CSS account but getting 403 from default-private folder
    - granted access to container via airlock but it doesn't apply transitively to the posts within
    - default-public also doesn't allow access to posts (regrant this)
    - claude added inheritance support, dig into what this actually does with regards to persistent ACLs
    - is airlock showing correct file permissions for ESS with inheritance policy in parent container ACR?
    - test all this works in reverse from ESS->CSS too
    - can we do groups with permissions? seems extreme to list all friends on a container
 - ensure it is obvious for a first time user what steps to follow
 - also remove connection
 - why is contacts.ttl in solid-social container in CSS? where should it go?
    - there's also one in podbay/comms/contacts.ttl
 - tests?
- revisit the tagging stuff
 - get claude to reapply the shelf (shelved to get connections working first in order to test)
 - don't forget the untracked mentions namespace too
- make it obvious which pod you are connected to on comms
    - a little tagline under the comms title or something, just to help me distinguish between CSS and ESS now they have display parity
- read all the code and tidy up anything that needs it
- add better styling
 - airlock back breadcrumb and make info panel icon more obvious
 - rename comms to so.fee?
- make 502 handling better (error message too vague, spinners and more retries)
- accessibility features
- pod hygiene app
 - checks for what data is where, type declarations and ACLs
 - extended profile shenanigans
    - profile should be public but use seealso for more private info to share with select people

